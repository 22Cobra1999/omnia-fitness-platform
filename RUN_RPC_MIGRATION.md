# Optimization Step: Create Availability RPC

To complete the optimization of the calendar availability calculation, you must run the following SQL migration in your Supabase SQL Editor.

This creates the `get_coach_availability_summary` function which powers the new fast frontend.

## Instructions
1. Open Supabase Dashboard > SQL Editor.
2. Run the content of `supabase/migrations/20260212_optimize_availability_rpc.sql`.

The file is located at:
`/Users/francopomati/.gemini/antigravity/scratch/omnia-fitness-platform/supabase/migrations/20260212_optimize_availability_rpc.sql`
